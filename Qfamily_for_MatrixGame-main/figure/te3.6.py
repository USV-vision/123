import torch as th
import torch.nn as nn
import torch.nn.functional as F
import numpy as np
import random
from torch.nn.parameter import Parameter
import torch.nn.init as init
import math
import random
#
# a = np.array([1, 2])
# print(*[a] * 3)
# cartesian_product = np.array(np.meshgrid(*[a] * 3)).T.reshape(-1, 3)
# b = np.array(np.meshgrid(*[a] * 3))
# print(b)
# print(cartesian_product)
# c = [1, 2]
# print(3**2)
a = random.randint(0, 3)
print(a)
